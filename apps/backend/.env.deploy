# Cloud Run / Cloud Build deploy configuration (read by deploy.sh).
# These are non-secret identifiers; the Turso token lives in Secret Manager.
GCP_PROJECT=juan-custom-apps
GCP_REGION=us-central1
AR_REPO=containers
SERVICE_NAME=expenser-backend
